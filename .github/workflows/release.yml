name: Release - Docker Image

on:
  push:
    tags: ['v*']

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Validate release
        run: |
          dotnet restore IgnakeeAI.McpServer.Supplier.sln
          dotnet build IgnakeeAI.McpServer.Supplier.sln -c Release --no-restore
          dotnet test IgnakeeAI.McpServer.Supplier.sln -c Release --no-build --verbosity normal

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: src/IgnakeeAI.McpServer.Supplier.Api/Dockerfile
          push: true
          tags: |
            ghcr.io/arlexrush/mcp-supplier-server:${{ steps.version.outputs.VERSION }}
            ghcr.io/arlexrush/mcp-supplier-server:latest
          platforms: linux/amd64,linux/arm64